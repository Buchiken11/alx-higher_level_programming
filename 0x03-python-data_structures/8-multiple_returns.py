#!/usr/bin/python3
def multiple_returns(sentence):
    for i in sentence:
        if len(sentence) < 0:
            return None
        length = len(sentence)
        first = sentence[0]
    else:
        return length, first
